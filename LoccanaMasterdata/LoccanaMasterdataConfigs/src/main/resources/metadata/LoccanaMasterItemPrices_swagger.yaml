openapi: 3.0.1
info:
  title: LoccanaMasterItemPrices
  description: API Definition of LoccanaMasterItemPrices
  version: 1.0.0
servers:
- url: http://localhost:8290/loccana/master/item_price
paths:
  /{id}:
    get:
      parameters:
      - name: id
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
  /:
    post:
      responses:
        default:
          description: Default response
